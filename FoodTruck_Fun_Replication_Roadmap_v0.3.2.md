# FoodTruck — 재미 복제 로드맵 (Fun Replication Roadmap)

**Status**: ✅ APPROVED (감리자 조건 충족 - PG-1+PG-2 MVP 착수 가능)
**Version**: v0.3.2
**Created**: 2026-01-20
**Updated**: 2026-01-20
**Author**: Claude (Implementer)
**Reviewer**: ChatGPT (Auditor), User (Owner)
**Approval**: ChatGPT (2026-01-20, v0.3 조건 충족 → v0.3.1 최종 반영)

---

## 0. Executive Summary

### 0.1 배경

현재 FoodTruck은 Papa's 게임의 **"시스템"**을 복제했으나, **"재미"**를 복제하지 못했다.

| 구분 | Papa's | 현재 FoodTruck |
|------|--------|----------------|
| 주문 시스템 | ✅ | ✅ |
| 재료/레시피 | ✅ | ✅ |
| 업그레이드 | ✅ | ✅ |
| Day End | ✅ | ✅ |
| **타이밍 미니게임** | ✅ | ❌ |
| **정밀도 챌린지** | ✅ | ❌ |
| **손님 캐릭터** | ✅ | ❌ |
| **대기 시간 압박** | ✅ | ❌ |
| **시각적 조리 과정** | ✅ | ❌ |
| **멀티태스킹** | ✅ | ❌ |

### 0.2 목표

> **Papa's 게임이 "재밌는 이유"를 분석하고, 그 핵심 요소를 FoodTruck에 이식한다.**

### 0.3 슬로건

#### Operational Slogan (기존 유지)
> "티켓을 읽고, 단계대로 만들고, 정확하게 서빙해서 팁을 받는다."

#### Fun Slogan (신규 추가)
> "타이밍 맞추고, 정확히 만들고, 손님 기다리게 하지 마라."

### 0.4 Phase 구성 개요

| Phase | 이름 | 핵심 목표 | 우선순위 |
|-------|------|----------|----------|
| PG-1 | Cook 미니게임 | 타이밍 게이지로 요리 | ⭐⭐⭐ |
| PG-2 | 손님 시스템 | 캐릭터 + 인내심 | ⭐⭐⭐ |
| PG-3 | Build 미니게임 | 재료 조립 인터랙션 | ⭐⭐ |
| PG-4 | Top 미니게임 | 토핑/마무리 정밀도 | ⭐⭐ |
| PG-5 | 시각적 피드백 | 애니메이션 + 이펙트 | ⭐ |
| PG-6 | 멀티태스킹 | 동시 주문 처리 | ⭐⭐ |
| PG-7 | Day 시스템 강화 | 진행감 + 목표 | ⭐ |
| PG-8 | 폴리시 | 사운드 + 주스 | ⭐ |

### 0.5 플랫폼 정책

| 항목 | 결정 |
|------|------|
| **우선순위** | 모바일 (터치) |
| **호환성** | PC (클릭/키보드) |

**설계 원칙:**
- 터치 = 클릭 동일 처리 (Input Agnostic)
- 모든 UI 요소는 터치 친화적으로 설계
- PC에서도 마우스 클릭으로 동일하게 동작

### 0.6 확정 결정사항 (Auditor 승인)

> 2026-01-20 ChatGPT 감리자 결정안 + Owner 승인

| 질문 | 결정 | 근거 |
|------|------|------|
| **Q1: Cook 입력 방식** | **단일 탭** | Papa's Mix 감각 복제, 모바일 친화 |
| **Q2: 점수 가중치** | **build(30) + cook(40) + top(20) + patience(10)** | Cook 중심, Patience 부가 압박 |
| **Q3: Rush 발동** | **큐 기반** (대기 손님 N명 초과) | 동적 난이도 조절, 학습 가능 |

**추가 정책:**
- 롱프레스는 PG-8(폴리시) 실험 옵션으로 보류
- 팁 계산은 점수와 분리: `tip = base * cookQuality * patienceBonus`

### 0.7 필수 조건 (MANDATORY)

> ChatGPT 감리자 지정 - 구현 전 반드시 반영

| # | 조건 | 상태 |
|---|------|------|
| M1 | 서버 검증 (클라이언트 판정 불신뢰) | 📋 §2.7 참조 |
| M2 | Patience Clamp + Rush 상한 | 📋 §3.7 참조 |
| M3 | 입력 딜레이/프레임레이트 보정 | 📋 §2.8 참조 |
| M4 | 텔레메트리 이벤트 정의 | 📋 §13 참조 |
| M5 | UI/UX 가이드라인 (모바일 우선) | 📋 §14 참조 |
| M6 | Exit Criteria 수치화 | 📋 각 Phase 참조 |

---

## 1. Papa's 재미 분석 (Reference Analysis)

### 1.1 Papa's Freezeria 플로우

```
┌─────────────────────────────────────────────────────────────────┐
│                    Papa's Freezeria 게임 루프                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [Order Station]                                                │
│  ├─ 손님 입장 (시각적 캐릭터)                                    │
│  ├─ 주문 확인 (말풍선 + 티켓)                                    │
│  ├─ 인내심 게이지 시작                                          │
│  └─ 티켓 → Build로 이동                                         │
│                                                                 │
│  [Build Station]                                                │
│  ├─ 컵/용기 선택                                                │
│  ├─ 재료 투입 (드래그)                                          │
│  ├─ 양 조절 (게이지/횟수)                                       │
│  └─ 완료 → Mix로 이동                                           │
│                                                                 │
│  [Mix Station]                                                  │
│  ├─ 믹서 시작                                                   │
│  ├─ 게이지 이동 (자동)                                          │
│  ├─ 타이밍에 멈추기 (플레이어 클릭)                              │
│  ├─ Perfect/Good/OK/Bad 판정                                    │
│  └─ 완료 → Top으로 이동                                         │
│                                                                 │
│  [Top Station]                                                  │
│  ├─ 휘핑크림 (드래그로 짜기)                                    │
│  ├─ 토핑 배치 (정확한 위치)                                     │
│  ├─ 시럽/소스 (드래그로 뿌리기)                                 │
│  └─ 완료 → Serve로 이동                                         │
│                                                                 │
│  [Serve Station]                                                │
│  ├─ 손님에게 전달                                               │
│  ├─ 각 항목별 채점 (Build/Mix/Top/Wait)                         │
│  ├─ 총점 + 팁 계산                                              │
│  ├─ 손님 반응 (표정/대사)                                       │
│  └─ 다음 손님                                                   │
│                                                                 │
│  [Day End]                                                      │
│  ├─ 하루 통계                                                   │
│  ├─ 총 팁/수입                                                  │
│  ├─ 해금 알림                                                   │
│  └─ 다음 Day                                                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 Papa's 핵심 재미 요소 (Fun Pillars)

| # | 재미 요소 | 설명 | 심리적 효과 |
|---|----------|------|------------|
| F1 | **타이밍 챌린지** | 정확한 순간에 클릭/멈추기 | 긴장감, 성취감 |
| F2 | **정밀도 챌린지** | 정확한 양, 정확한 위치 | 집중력, 만족감 |
| F3 | **시간 압박** | 손님 대기 → 점수 감소 | 긴박감, 우선순위 판단 |
| F4 | **멀티태스킹** | 여러 주문 동시 처리 | 몰입, 도전 |
| F5 | **시각적 피드백** | 음식이 만들어지는 과정 | 만족감, 몰입 |
| F6 | **진행감** | 해금, 레벨업, 새 콘텐츠 | 동기부여, 목표 |
| F7 | **즉각적 보상** | 점수 팝업, 팁 애니메이션 | 도파민, 중독성 |
| F8 | **실패 허용** | 실패해도 계속 가능 | 스트레스 감소, 재시도 |

### 1.3 현재 FoodTruck 분석

```
현재 플로우:

[Order]          [Craft]           [Deliver]
주문 확인    →   버튼 클릭    →    버튼 클릭
   │               │                 │
   │               │                 │
   ▼               ▼                 ▼
 (끝)          (5초 대기)          (끝)
                   │
                   ▼
              요리 완성
```

**문제점:**
- 플레이어 인터랙션이 "버튼 클릭" 2번으로 끝남
- 타이밍/정밀도 요소 없음
- 손님이 보이지 않음 (추상적 주문만 존재)
- 대기 시간 패널티 없음
- 시각적 피드백 최소

### 1.4 재미 복제 Gap 분석

| Papa's 요소 | 현재 상태 | Gap | 해결 Phase |
|-------------|----------|-----|-----------|
| 타이밍 게이지 | ❌ 없음 | 🔴 Critical | PG-1 |
| 손님 캐릭터 | ❌ 없음 | 🔴 Critical | PG-2 |
| 인내심 시스템 | ❌ 없음 | 🔴 Critical | PG-2 |
| 재료 조립 | ❌ 자동 | 🟡 High | PG-3 |
| 토핑 배치 | ❌ 없음 | 🟡 High | PG-4 |
| 조리 애니메이션 | ❌ 없음 | 🟡 High | PG-5 |
| 멀티태스킹 | ❌ 순차만 | 🟡 High | PG-6 |
| Day 목표 | 🔶 부분 | 🟢 Medium | PG-7 |
| 사운드/이펙트 | ❌ 없음 | 🟢 Medium | PG-8 |

---

## 2. Phase PG-1: Cook/Mix 미니게임

### 2.1 개요

| 항목 | 내용 |
|------|------|
| **목표** | craftTime 동안 단순 대기 → 타이밍 미니게임으로 전환 |
| **핵심 메커니즘** | 게이지가 이동하고, 플레이어가 정확한 타이밍에 멈춤 |
| **영향 범위** | CraftingService, CraftPanel UI |
| **우선순위** | ⭐⭐⭐ 최우선 |

### 2.2 게임플레이 상세

#### 2.2.1 기본 메커니즘

```
┌─────────────────────────────────────────────────────────────┐
│                     Cook 미니게임 UI                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│    [요리 이름: Cheese Burger]                               │
│                                                             │
│    ┌─────────────────────────────────────────────────┐     │
│    │░░░░░░░░░░░░░░█████░░░░░░░░░░░░░░░░░░░░░░░░░░░░│     │
│    └─────────────────────────────────────────────────┘     │
│                      ↑                                      │
│                 [초록 영역]                                 │
│                  Perfect                                    │
│                                                             │
│              ●━━━━━━━━━━━━━━━━━━━▶                         │
│              ↑                                              │
│         [이동하는 포인터]                                    │
│                                                             │
│              [ 🔥 COOK! ] ← 클릭 버튼                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 2.2.2 판정 시스템

| 판정 | 조건 | 점수 배율 | 시각 피드백 |
|------|------|----------|------------|
| **Perfect** | 초록 영역 중앙 | 100% | 🌟 + 화면 흔들림 |
| **Great** | 초록 영역 | 90% | ⭐ |
| **Good** | 노란 영역 | 75% | ✓ |
| **OK** | 주황 영역 | 50% | - |
| **Bad** | 빨간 영역 | 25% | ✗ |

#### 2.2.2a 실패 정의 (Owner 결정 2026-01-20)

> **Bad 판정 = 점수/팁 감소 + 표정 악화, 플레이는 계속**

| Bad 판정 시 | 결과 |
|------------|------|
| 점수 | cookScore × 0.25 (최저) |
| 팁 | 기본 팁에서 감소 |
| 손님 표정 | 악화 (PG-2에서 구현) |
| **플레이** | **계속 진행** (Game Over 없음) |

> **근거**: Anti-Pattern "강제 시간 제한 / Game Over 금지" 준수
> **참조**: Papa's 원작도 실패 시 계속 진행 가능

**Bad 누적 정책 (Owner 결정 2026-01-20):**
- **추가 영향 없음** - 각 판정은 매회 독립
- Bad 3회 연속이어도 추가 페널티 없음
- 누적 로직은 PG-2 (손님/표정/인내심)에서 확장 가능

#### 2.2.2b Perfect 보호 구간 (Owner 채택 2026-01-20)

> **서버 판정이 클라이언트보다 낮을 때, 중심 ±ε 이내면 보호**

```lua
-- Perfect 보호 구간 적용
local PROTECTION_EPSILON = 1  -- ±1 position
local PERFECT_CENTER = 50     -- 중앙

function applyProtection(serverJudgment, clientJudgment, serverGaugePos)
    -- 클라가 Perfect인데 서버가 Great로 내려갈 때만 적용
    if clientJudgment == "Perfect" and serverJudgment == "Great" then
        local distFromCenter = math.abs(serverGaugePos - PERFECT_CENTER)
        if distFromCenter <= PROTECTION_EPSILON then
            return "Perfect"  -- 보호
        end
    end
    return serverJudgment  -- 원래 판정 유지
end
```

**효과:**
- "서버가 빼앗았다" 인식 감소
- 네트워크 환경 불만 최소화
- 리듬/타이밍 게임 표준 UX

#### 2.2.3 난이도 차등

| 난이도 | 게이지 속도 | 초록 영역 크기 | 왕복 여부 |
|--------|------------|---------------|----------|
| Easy | 느림 (2초/왕복) | 넓음 (20%) | 1회 |
| Medium | 보통 (1.5초/왕복) | 보통 (15%) | 1~2회 |
| Hard | 빠름 (1초/왕복) | 좁음 (10%) | 2~3회 |

#### 2.2.4 게이지 동작

```lua
-- 게이지 동작 pseudocode
local gauge = {
    position = 0,        -- 0 ~ 100
    direction = 1,       -- 1 또는 -1
    speed = 50,          -- position/초
    targetZone = {40, 60}, -- Perfect 영역
}

-- 매 프레임
gauge.position = gauge.position + (gauge.direction * gauge.speed * dt)

-- 끝에 도달하면 반전
if gauge.position >= 100 or gauge.position <= 0 then
    gauge.direction = gauge.direction * -1
end

-- 플레이어 클릭 시
function onCookClick()
    local pos = gauge.position
    if pos >= 45 and pos <= 55 then
        return "Perfect"
    elseif pos >= 40 and pos <= 60 then
        return "Great"
    -- ... etc
    end
end
```

### 2.3 데이터 구조 변경

#### 2.3.1 Recipe 확장 (선택)

```lua
-- GameData.lua (선택적 확장)
Recipes = {
    [1] = {
        id = 1,
        name = "Basic Burger",
        -- 기존 필드...

        -- PG-1 추가 필드 (선택)
        cookDifficulty = "Easy",  -- Easy/Medium/Hard
        gaugeSpeed = 50,          -- 커스텀 속도 (선택)
        perfectZoneSize = 20,     -- 커스텀 영역 (선택)
    },
}
```

#### 2.3.2 기본값 정책

- cookDifficulty 필드 없으면 → Recipe.difficulty 참조
- gaugeSpeed 필드 없으면 → 난이도별 기본값 사용
- **기존 레시피 30개는 수정 불필요** (기본값으로 동작)

### 2.4 UI 변경

| 기존 | 변경 후 |
|------|--------|
| "Crafting..." 텍스트 + 프로그레스 바 | 타이밍 게이지 UI |
| craftTime 후 자동 완료 | 플레이어 클릭으로 완료 |
| 성공/실패만 존재 | Perfect~Bad 5단계 판정 |

### 2.5 서버 연동

```
Client                          Server
   │                               │
   │  RE_CraftItem                 │
   │  {recipeId, slotIndex}        │
   │  ──────────────────────────►  │
   │                               │ 재료 검증
   │                               │ 재료 차감
   │  {Success, craftStartTime}    │
   │  ◄──────────────────────────  │
   │                               │
   │  (클라이언트 미니게임 진행)    │
   │                               │
   │  RE_CraftComplete             │
   │  {slotIndex, judgment}        │
   │  ──────────────────────────►  │
   │                               │ judgment 검증
   │                               │ 요리 품질 저장
   │  {Success, cookScore}         │
   │  ◄──────────────────────────  │
```

### 2.6 Exit Criteria

| ID | 체크 항목 | PASS 기준 |
|----|----------|----------|
| PG1-01 | 게이지 UI 표시 | Craft 시작 시 게이지 표시 |
| PG1-02 | 게이지 이동 | 좌우로 부드럽게 이동 (60fps 기준) |
| PG1-03 | 클릭 판정 | Perfect~Bad 5단계 판정 |
| PG1-04 | 난이도 차등 | Easy/Medium/Hard 속도 다름 |
| PG1-05 | 점수 연동 | 판정 → cookScore 반영 |
| PG1-06 | 서버 검증 | 서버 독립 재현으로 판정 (§2.7) |
| **PG1-07** | **Perfect 목표 (난이도별)** | 아래 표 참조 |
| **PG1-08** | **반응성 (클라/서버 분리)** | 아래 표 참조 |

#### PG1-07 난이도별 Perfect 목표

| 구간 | Perfect 목표 | 설명 |
|------|-------------|------|
| **튜토리얼** (Day 1, 처음 3판) | 60~80% | 초반 이탈 방지, 성공 체험 |
| **일반** (Day 1~5, Easy/Medium) | 30~50% | 적절한 도전감 |
| **Hard** (Day 6+, Hard 레시피) | 10~25% | 숙련자 대상, 고난이도 |

> **Note**: §13.4의 분석 지표 "Perfect 30~50%"는 **일반 구간 기준**입니다.

#### PG1-08 반응성 기준 (클라/서버 분리)

| 구분 | 기준 | 설명 |
|------|------|------|
| **클라 피드백** | < 50ms | 터치 → 임시 판정 연출 (로컬) |
| **서버 확정** | < 300ms | 서버 왕복 후 최종 판정 |
| **보정 처리** | 필요 시 | 서버 판정 ≠ 임시 판정이면 UI 수정 |

> **설계**: 클라는 즉시 "임시 Perfect!" 연출 → 서버 확정 → 다르면 조용히 보정

### 2.7 서버 검증 규격 (M1) - 서버 독립 재현 방식

> ⚠️ **필수 조건**: 클라이언트 데이터를 신뢰하지 않는다. 서버가 게이지 위치를 **독립적으로 재현**한다.

#### 2.7.1 설계 원칙

```
┌─────────────────────────────────────────────────────────────┐
│  ❌ 취약한 설계 (v0.2 이전)                                  │
│  클라: gaugePosition=50 전송 → 서버: 그대로 믿고 Perfect     │
│  → 클라가 조작하면 끝                                        │
├─────────────────────────────────────────────────────────────┤
│  ✅ 안전한 설계 (v0.3)                                       │
│  서버: seed+startTime 저장 → 클라: tapTime만 전송            │
│  → 서버가 elapsed로 gaugePosition 독립 계산                  │
│  → 클라가 뭘 보내든 무시                                     │
└─────────────────────────────────────────────────────────────┘
```

#### 2.7.2 서버 상태 저장 (CraftStart 시점)

```lua
-- CraftingService.lua
function CraftingService:StartCraft(player, slotIndex, recipeId)
    -- 서버에서 게이지 파라미터 결정 (클라 조작 불가)
    local seed = math.random(1, 1000000)
    local startTime = os.clock()

    -- 레시피 난이도에 따른 게이지 설정
    local recipe = GameData.Recipes[recipeId]
    local gaugeConfig = self:GetGaugeConfig(recipe.difficulty)

    -- 서버에 상태 저장 (v0.3.1: isTutorial 추가)
    local playerData = DataService:GetPlayerData(player)
    self.craftingSessions[player.UserId] = {
        slotIndex = slotIndex,
        recipeId = recipeId,
        seed = seed,
        serverStartTime = startTime,
        gaugeSpeed = gaugeConfig.speed,
        targetZone = gaugeConfig.targetZone,
        initialDirection = (seed % 2 == 0) and 1 or -1,
        isTutorial = not playerData.tutorialComplete,  -- v0.3.1: 튜토리얼 여부
        -- Owner 결정 (2026-01-20): 기존 tutorialComplete 플래그 그대로 사용
        -- Day 1 종료/Perfect 1회 등 별도 기준 없음 (기존 시스템과 정합성)
    }

    -- 클라에게 시각적 표시용 데이터만 전송 (v0.3.1: 서버 시간 미노출)
    return {
        Success = true,
        GaugeConfig = gaugeConfig,  -- UI 표시용
        -- ServerStartTime 제거: 클라이언트에 서버 내부 시간 노출 금지
        -- 클라는 자체 os.clock() 기준으로 게이지 애니메이션 표시
    }
end
```

#### 2.7.3 서버 독립 재현 (CraftComplete 시점)

```lua
-- CraftingService.lua
function CraftingService:CompleteCraft(player, clientTapTime)
    local session = self.craftingSessions[player.UserId]
    if not session then
        return { Success = false, Error = "NO_SESSION" }
    end

    -- 1. 서버 시간 기준 elapsed 계산
    local serverNow = os.clock()
    local elapsed = serverNow - session.serverStartTime

    -- 2. 시간 범위 검증
    if elapsed < MIN_COOK_TIME or elapsed > MAX_COOK_TIME then
        return { Success = false, Error = "TIMING_INVALID" }
    end

    -- 3. 서버에서 게이지 위치 독립 계산 (클라 데이터 무시)
    local serverGaugePos = self:CalculateGaugePosition(
        elapsed,
        session.gaugeSpeed,
        session.initialDirection
    )

    -- 4. 네트워크 지연 보정 (±80ms 윈도우) + 튜토리얼 확대
    local judgment = self:CalculateJudgmentWithTolerance(
        serverGaugePos,
        session.targetZone,
        session.gaugeSpeed,  -- v0.3.1: gaugeSpeed 인자 추가
        LATENCY_WINDOW_MS,
        session.isTutorial   -- v0.3.1: 튜토리얼 여부 (Perfect ±8 vs ±5)
    )

    -- 5. 세션 정리
    self.craftingSessions[player.UserId] = nil

    return {
        Success = true,
        Judgment = judgment,
        CookScore = JUDGMENT_SCORES[judgment],
    }
end
```

#### 2.7.4 게이지 위치 계산 (서버 독립)

```lua
-- 서버에서 elapsed 시간으로 게이지 위치 계산
function CraftingService:CalculateGaugePosition(elapsed, speed, initialDir)
    -- 게이지는 0~100 사이를 왕복
    local totalDistance = elapsed * speed
    local cycles = math.floor(totalDistance / 100)
    local remainder = totalDistance % 100

    -- 방향 결정 (짝수 사이클 = 초기 방향, 홀수 = 반대)
    local currentDir = (cycles % 2 == 0) and initialDir or -initialDir

    if currentDir == 1 then
        return remainder  -- 0 → 100 방향
    else
        return 100 - remainder  -- 100 → 0 방향
    end
end
```

#### 2.7.5 네트워크 지연 보정

```lua
-- 지연 보정 윈도우 적용
LATENCY_WINDOW_MS = 80  -- ±80ms 허용

-- 판정 구간 (v0.3.1: 튜토리얼 확대)
JUDGMENT_ZONES = {
    tutorial = { Perfect = 8, Great = 13, Good = 18, OK = 28 },  -- 확대
    normal   = { Perfect = 5, Great = 10, Good = 15, OK = 25 },  -- 기본
}

function CraftingService:CalculateJudgmentWithTolerance(gaugePos, targetZone, gaugeSpeed, latencyMs, isTutorial)
    local targetCenter = (targetZone[1] + targetZone[2]) / 2
    local distance = math.abs(gaugePos - targetCenter)

    -- 지연 보정: 거리에서 보정값 차감 (v0.3.1: gaugeSpeed 인자로 전달)
    local latencyCompensation = (latencyMs / 1000) * gaugeSpeed * 0.5
    local adjustedDistance = math.max(0, distance - latencyCompensation)

    -- 판정 구간 선택 (v0.3.1: 튜토리얼에서 확대)
    local zones = isTutorial and JUDGMENT_ZONES.tutorial or JUDGMENT_ZONES.normal

    -- 판정
    if adjustedDistance <= zones.Perfect then return "Perfect"
    elseif adjustedDistance <= zones.Great then return "Great"
    elseif adjustedDistance <= zones.Good then return "Good"
    elseif adjustedDistance <= zones.OK then return "OK"
    else return "Bad"
    end
end
```

> **감리자 필수 조건**: 튜토리얼 Perfect ±8, 일반 ±5. 수치는 PG1-07 달성률 보고 조정 가능.

#### 2.7.6 검증 임계값

| 항목 | 값 | 설명 |
|------|-----|------|
| MIN_COOK_TIME | 1.5초 | 최소 조리 시간 |
| MAX_COOK_TIME | 30초 | 최대 조리 시간 |
| LATENCY_WINDOW_MS | 80ms | 네트워크 지연 보정 윈도우 |
| SESSION_TIMEOUT | 60초 | 세션 만료 시간 |

#### 2.7.7 클라이언트 역할 (UI Only) - v0.3.1 확장

> ⚠️ **감리자 필수 조건**: 임시 판정은 UI 전용, 보상/점수는 서버 확정만

**원칙:**
| 항목 | 클라 임시 판정 | 서버 확정 판정 |
|------|---------------|---------------|
| 애니메이션/텍스트 | ✅ 사용 | ✅ 최종 |
| 점수(cookScore) | ❌ 금지 | ✅ 진실 |
| 팁 계산 | ❌ 금지 | ✅ 진실 |
| 진행 상태 변경 | ❌ 금지 | ✅ 진실 |

```lua
-- 클라이언트: 로컬 게이지로 임시 판정 (UI Only)
local clientJudgment = self:CalculateLocalJudgment(gaugePosition)

-- 임시 판정 UI 표시 (즉각 피드백)
self:ShowTemporaryJudgment(clientJudgment)

-- 서버 요청
RE_CraftComplete:FireServer({
    clientTapTime = os.clock(),  -- 참고용 (선택적)
    clientJudgment = clientJudgment,  -- 텔레메트리용 (선택적)
})

-- 서버 응답 수신
RE_CraftComplete.OnClientEvent:Connect(function(response)
    local serverJudgment = response.Judgment

    -- 보정 필요 여부 확인
    if serverJudgment ~= clientJudgment then
        -- 보정 방향에 따라 처리
        if IsUpCorrect(clientJudgment, serverJudgment) then
            -- Up-correct: 클라 Good → 서버 Great (자연스럽게 업)
            self:AnimateJudgmentUp(clientJudgment, serverJudgment)
        else
            -- Down-correct: 클라 Perfect → 서버 Great (묵시적 보정)
            self:AnimateJudgmentDown(clientJudgment, serverJudgment)
        end
    end

    -- ⚠️ 점수/팁/진행은 서버 확정만 사용
    self.cookScore = response.CookScore  -- 서버가 준 값만 사용
end)
```

**보정 애니메이션 정책:**
| 방향 | 예시 | 처리 |
|------|------|------|
| Up | Good → Great | 텍스트 "↑" 효과 + 색상 업그레이드 (긍정적) |
| Down | Perfect → Great | 텍스트 조용히 교체 (§2.8.4 절충안) |

### 2.8 입력 보정 정책 (M3)

> ⚠️ **필수 조건**: 저프레임/네트워크 지연에도 공정한 판정

#### 2.8.1 프레임레이트 보정

```lua
-- 게이지 업데이트는 deltaTime 기반 (프레임 독립)
function updateGauge(dt)
    -- 프레임레이트 관계없이 동일한 속도
    gauge.position = gauge.position + (gauge.direction * gauge.speed * dt)
end
```

#### 2.8.2 입력 지연 허용

```lua
-- 판정 영역을 약간 확장하여 지연 보정
local LATENCY_TOLERANCE = 3  -- 게이지 위치 ±3 허용

function CalculateJudgment(position)
    local targetCenter = 50
    local distance = math.abs(position - targetCenter)

    -- 지연 보정 적용
    distance = math.max(0, distance - LATENCY_TOLERANCE)

    if distance <= 5 then return "Perfect"
    elseif distance <= 10 then return "Great"
    elseif distance <= 15 then return "Good"
    elseif distance <= 25 then return "OK"
    else return "Bad"
    end
end
```

#### 2.8.3 모바일 터치 보정

| 항목 | 정책 |
|------|------|
| 터치 영역 | 최소 44x44 픽셀 (iOS HIG) |
| 터치 피드백 | 즉각적 시각 피드백 (하이라이트) |
| 더블탭 방지 | 탭 후 200ms 내 재탭 무시 |

#### 2.8.4 클라이언트-서버 보정 UX 정책 (v0.3.1)

> **결정**: 절충안 채택 (ChatGPT 감리자 2026-01-20)

서버 독립 재현 방식(§2.7)으로 인해 클라이언트 표시와 서버 판정이 다를 수 있다.
이 불일치를 사용자에게 어떻게 전달할지에 대한 UX 정책:

| 옵션 | 설명 | 채택 |
|------|------|------|
| A. Silent | 서버 판정 우선, 사용자 미고지 | ❌ |
| B. Transparent | 클라/서버 판정 둘 다 표시 | ❌ |
| **C. Compromise** | **튜토리얼 1회 안내 + 이후 묵시적** | ✅ |

**절충안 구현:**

```lua
-- 튜토리얼 중 최초 1회만 안내
local hasShownNetworkNotice = false

function ShowJudgmentResult(clientJudgment, serverJudgment)
    -- 항상 서버 판정 표시
    DisplayJudgment(serverJudgment)

    -- 불일치 시 & 미안내 시 → 안내
    if clientJudgment ~= serverJudgment and not hasShownNetworkNotice then
        ShowNotice("network_adjustment_tutorial")
        -- "네트워크 상태에 따라 판정이 보정될 수 있습니다"
        hasShownNetworkNotice = true
    end

    -- 이후 불일치는 묵시적 처리 (서버 판정만 표시)
end
```

**안내 문구 예시:**
- KO: "네트워크 상태에 따라 판정이 조정될 수 있습니다."
- EN: "Judgment may be adjusted based on network conditions."

**근거:**
- 투명성: 시스템 동작을 최소 1회 설명
- UX: 매번 알림 시 피로감 방지
- 공정성: 서버 판정 = 진실(ground truth)

---

## 3. Phase PG-2: 손님 시스템

### 3.1 개요

| 항목 | 내용 |
|------|------|
| **목표** | 추상적 주문 → 실제 손님 캐릭터 + 인내심 |
| **핵심 메커니즘** | 손님 시각화, 대기 시간에 따른 만족도 감소 |
| **영향 범위** | OrderService, OrderBoard UI, 신규 Customer 시스템 |
| **우선순위** | ⭐⭐⭐ 최우선 |

### 3.2 손님 캐릭터

#### 3.2.1 손님 데이터 구조

```lua
-- GameData.lua 또는 별도 CustomerData.lua
Customers = {
    [1] = {
        id = 1,
        name = "Regular Joe",
        nameKo = "단골 조",

        -- 외형
        appearance = {
            body = "body_01",
            head = "head_01",
            color = Color3.fromRGB(255, 200, 150),
        },

        -- 성격 (v0.3: patienceMax 모델)
        patienceMax = 100,       -- 최대 인내심 (손님별)
        patienceDecayRate = 1,   -- 초당 감소량
        tipMultiplier = 1.0,     -- 팁 배율

        -- 해금 조건
        unlockDay = 1,           -- Day 1부터 등장

        -- 선호도 (선택)
        preferences = {
            favoriteRecipes = {1, 3, 5},
            dislikedRecipes = {},
        },
    },

    [2] = {
        id = 2,
        name = "Busy Bob",
        nameKo = "바쁜 밥",
        patienceMax = 60,        -- 성격 급함 (최대치 낮음)
        patienceDecayRate = 2,   -- 빨리 감소
        tipMultiplier = 1.5,     -- 대신 팁 많음
        unlockDay = 3,
    },

    [3] = {
        id = 3,
        name = "Patient Patty",
        nameKo = "느긋한 패티",
        patienceMax = 150,       -- 느긋함 (최대치 높음)
        patienceDecayRate = 0.5,
        tipMultiplier = 0.8,     -- 팁 적음
        unlockDay = 5,
    },

    -- ... 더 많은 손님
}
```

#### 3.2.2 손님 타입

| 타입 | patienceMax | 감소 속도 | 팁 배율 | 특징 |
|------|-------------|----------|---------|------|
| **Regular** | 100 | 1/초 | 1.0x | 기본 손님 |
| **Busy** | 60 | 2/초 | 1.5x | 급하지만 팁 많음 |
| **Patient** | 150 | 0.5/초 | 0.8x | 느긋하지만 팁 적음 |
| **VIP** | 80 | 1.5/초 | 2.0x | 까다롭지만 팁 최고 |
| **Kid** | 50 | 3/초 | 0.5x | 매우 급함, 팁 적음 |
| **Elder** | 200 | 0.3/초 | 1.0x | 매우 느긋 |

> **Note (v0.3)**: 인내심은 손님별 `patienceMax`까지 가능. UI는 `currentPatience/patienceMax`로 % 표시.

### 3.3 인내심 시스템

#### 3.3.1 인내심 게이지

```
┌─────────────────────────────────────────────────────────────┐
│                      손님 카드 UI                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│     ┌─────────┐                                             │
│     │  😊     │  "단골 조"                                  │
│     │ /    \  │                                             │
│     │  ====   │  [Cheese Burger]                            │
│     └─────────┘                                             │
│                                                             │
│     인내심: [████████████████░░░░] 80%                      │
│                                                             │
│     보상: $15 + 팁                                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 3.3.2 인내심 상태

| 인내심 | 표정 | 색상 | 팁 영향 |
|--------|------|------|---------|
| 100~80% | 😊 | 초록 | +20% |
| 79~60% | 🙂 | 연초록 | +10% |
| 59~40% | 😐 | 노랑 | 0% |
| 39~20% | 😕 | 주황 | -20% |
| 19~1% | 😠 | 빨강 | -50% |
| 0% | 😡 | 빨강 (점멸) | -100% (팁 없음) |

#### 3.3.3 인내심 계산

```lua
-- 매 프레임 업데이트
function updateCustomerPatience(customer, dt)
    -- 기본 감소
    local decay = customer.patienceDecayRate * dt

    -- 상태에 따른 보정
    if customer.orderState == "COOKING" then
        decay = decay * 0.5  -- 요리 중이면 감소 느림
    elseif customer.orderState == "WAITING" then
        decay = decay * 1.0  -- 대기 중 기본
    end

    customer.currentPatience = math.max(0, customer.currentPatience - decay)

    -- 표정 업데이트
    updateCustomerExpression(customer)
end

-- 팁 계산 시 인내심 반영 (v0.3: % 기반)
function calculateTip(baseReward, cookScore, customer)
    -- 인내심을 % 로 변환
    local patiencePercent = (customer.currentPatience / customer.patienceMax) * 100
    local patienceBonus = 1.0

    if patiencePercent >= 80 then
        patienceBonus = 1.2
    elseif patiencePercent >= 60 then
        patienceBonus = 1.1
    elseif patiencePercent >= 40 then
        patienceBonus = 1.0
    elseif patiencePercent >= 20 then
        patienceBonus = 0.8
    else
        patienceBonus = 0.5
    end

    return math.floor(baseReward * (cookScore / 100) * patienceBonus)
end
```

### 3.4 손님 비주얼

#### 3.4.1 2D vs 3D 선택

| 옵션 | 장점 | 단점 | 권장 |
|------|------|------|------|
| **2D 이미지** | 제작 쉬움, 리소스 적음 | Roblox 스타일과 안 맞음 | 초기 MVP |
| **3D 캐릭터** | Roblox 네이티브, 확장성 | 제작 복잡 | 장기 목표 |
| **간단 3D** | 기본 Roblox 캐릭터 커스텀 | 중간 | ⭐ 권장 |

#### 3.4.2 권장 구현 (간단 3D)

```lua
-- Roblox 기본 캐릭터 기반 손님
local function createCustomerVisual(customerData)
    local model = Instance.new("Model")

    -- 기본 Rig 또는 간단한 Part 조합
    local body = Instance.new("Part")
    body.Size = Vector3.new(2, 3, 1)
    body.Color = customerData.appearance.color
    body.Parent = model

    -- 얼굴 (Decal 또는 BillboardGui)
    local face = Instance.new("BillboardGui")
    face.Size = UDim2.new(2, 0, 2, 0)
    -- 표정 이미지 설정

    return model
end
```

### 3.5 주문 흐름 변경

#### 3.5.1 기존 흐름

```
RefillOrders() → 랜덤 레시피 3개 생성 → UI 표시
```

#### 3.5.2 새 흐름

```
SpawnCustomer() → 손님 생성 → 손님이 레시피 선택 → 주문 등록
     │
     ├─ 손님 외형 결정
     ├─ 인내심 시작
     ├─ 주문대에 배치
     └─ 대기 시작
```

### 3.6 Exit Criteria

| ID | 체크 항목 | PASS 기준 |
|----|----------|----------|
| PG2-01 | 손님 캐릭터 표시 | 주문 시 손님 비주얼 표시 |
| PG2-02 | 인내심 게이지 | 시간에 따라 감소 |
| PG2-03 | 표정 변화 | 인내심에 따른 표정 (5단계) |
| PG2-04 | 팁 연동 | 인내심 → 팁 영향 |
| PG2-05 | 손님 다양성 | 최소 5종 손님 타입 |
| PG2-06 | Day 해금 | Day에 따라 새 손님 |
| **PG2-07** | **Rush 발동** | **대기 손님 3명 시 Rush 시작** |
| **PG2-08** | **Rush 제한** | **Day당 최대 3회, 쿨다운 40초** |
| **PG2-09** | **Patience 범위** | **currentPatience ∈ [0, patienceMax], UI는 % 표시** |

### 3.7 Rush 정책 (M2 - Part A)

> ⚠️ **필수 조건**: Rush 폭주 방지를 위한 상한 설정

#### 3.7.1 Rush 설정값

```lua
-- ServerFlags.lua 또는 별도 config
RUSH_CONFIG = {
    trigger_queue = 3,            -- 대기 손님 3명 이상 시 Rush
    duration = 20,                -- Rush 지속 20초
    cooldown = 40,                -- Rush 후 쿨다운 40초
    max_per_day = 3,              -- Day당 최대 Rush 3회
    patience_decay_bonus = 0.25,  -- Rush 중 인내심 감소 +25%
}
```

#### 3.7.2 Rush 상태 머신

```
[NORMAL] ──► 대기 손님 ≥ 3 ──► [RUSH]
                                  │
                               20초 경과
                                  │
                                  ▼
                            [COOLDOWN]
                                  │
                               40초 경과
                                  │
                                  ▼
                             [NORMAL]
```

#### 3.7.3 Rush 중 변경사항

| 항목 | Normal | Rush |
|------|--------|------|
| 인내심 감소율 | 1x | 1.25x |
| 손님 도착 간격 | 기본 | 50% 단축 |
| UI 표시 | 없음 | "🔥 RUSH!" 배너 |

#### 3.7.4 Rush 제한 (Anti-Abuse)

```lua
function CanTriggerRush(dayState)
    -- Day당 최대 3회
    if dayState.rushCount >= RUSH_CONFIG.max_per_day then
        return false
    end

    -- 쿨다운 중
    if dayState.rushCooldownEnd and os.clock() < dayState.rushCooldownEnd then
        return false
    end

    return true
end
```

### 3.8 Patience 정책 (M2 - Part B) - 손님별 patienceMax 모델

> ⚠️ **필수 조건**: 인내심은 손님별 최대값(patienceMax)을 가진다

#### 3.8.1 설계 원칙

```
┌─────────────────────────────────────────────────────────────┐
│  ❌ v0.2 이전 (모순 상태)                                    │
│  손님 데이터: patience=150 (느긋한 패티)                     │
│  Clamp: 0~100 고정 → 150이 100으로 잘림 → 개성 무의미        │
├─────────────────────────────────────────────────────────────┤
│  ✅ v0.3 (손님별 patienceMax)                                │
│  손님 데이터: patienceMax=150, currentPatience=150           │
│  Clamp: 0~patienceMax → 손님 개성 보존                       │
│  UI: currentPatience / patienceMax * 100 = % 표시            │
└─────────────────────────────────────────────────────────────┘
```

#### 3.8.2 손님 데이터 구조 (수정)

```lua
Customers = {
    [1] = {
        id = 1,
        name = "Regular Joe",
        patienceMax = 100,           -- 최대 인내심
        patienceDecayRate = 1,       -- 초당 감소량
        -- currentPatience는 런타임에 patienceMax로 초기화
    },
    [2] = {
        id = 2,
        name = "Busy Bob",
        patienceMax = 60,            -- 급한 손님
        patienceDecayRate = 2,
    },
    [3] = {
        id = 3,
        name = "Patient Patty",
        patienceMax = 150,           -- 느긋한 손님
        patienceDecayRate = 0.5,
    },
}
```

#### 3.8.3 Clamp 규칙 (수정)

```lua
-- 손님별 patienceMax 적용
function ClampPatience(customer, value)
    return math.max(0, math.min(customer.patienceMax, value))
end

-- 초기화
function InitializeCustomer(customer)
    customer.currentPatience = customer.patienceMax
end

-- 인내심 변경
customer.currentPatience = ClampPatience(customer, customer.currentPatience - decay)
```

#### 3.8.4 UI 표시 (% 변환)

```lua
-- UI는 항상 0~100% 범위로 표시
function GetPatiencePercent(customer)
    return (customer.currentPatience / customer.patienceMax) * 100
end

-- 예시:
-- Busy Bob: 30/60 = 50%
-- Patient Patty: 75/150 = 50%
-- → 둘 다 UI에서 50%로 표시되지만, Patty는 실제로 더 오래 버팀
```

#### 3.8.5 Patience 바운더리

| 상황 | 정책 |
|------|------|
| currentPatience < 0 | 0으로 Clamp |
| currentPatience > patienceMax | patienceMax로 Clamp |
| currentPatience = 0 | 손님 최대 불만 (팁 없음, 표정 😡) |
| currentPatience = 0 지속 10초 | 손님 이탈 (선택적) |

#### 3.8.6 Patience 변경 로그

```lua
-- 디버깅/밸런싱용 로그
function SetPatience(customer, newValue, reason)
    local oldValue = customer.currentPatience
    customer.currentPatience = ClampPatience(customer, newValue)

    if DEBUG_PATIENCE then
        local oldPct = math.floor((oldValue / customer.patienceMax) * 100)
        local newPct = math.floor((customer.currentPatience / customer.patienceMax) * 100)
        print(string.format(
            "[PATIENCE] %s: %d/%d (%d%%) → %d/%d (%d%%) [%s]",
            customer.name,
            oldValue, customer.patienceMax, oldPct,
            customer.currentPatience, customer.patienceMax, newPct,
            reason
        ))
    end
end
```

---

## 4. Phase PG-3: Build 미니게임

### 4.1 개요

| 항목 | 내용 |
|------|------|
| **목표** | 재료 자동 소모 → 직접 조립하는 인터랙션 |
| **핵심 메커니즘** | 재료 드래그/클릭, 순서 중요, 양 조절 |
| **영향 범위** | CraftPanel UI, CraftingService |
| **우선순위** | ⭐⭐ 높음 |

### 4.2 게임플레이 상세

#### 4.2.1 Build UI 레이아웃

```
┌─────────────────────────────────────────────────────────────┐
│                      Build Station                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  [레시피 가이드]              [조립 영역]                    │
│  ┌───────────┐              ┌───────────┐                  │
│  │ 1. Bun    │              │           │                  │
│  │ 2. Patty  │              │   🍞      │ ← 현재 상태      │
│  │ 3. Cheese │              │   🥩      │                  │
│  │ 4. Lettuce│              │   🧀      │                  │
│  │ 5. Bun    │              │   (다음?) │                  │
│  └───────────┘              └───────────┘                  │
│                                                             │
│  [재료 선택]                                                │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐               │
│  │ 🍞 │ │ 🥩 │ │ 🧀 │ │ 🥬 │ │ 🍅 │ │ 🥓 │               │
│  │ x5 │ │ x3 │ │ x4 │ │ x6 │ │ x2 │ │ x1 │               │
│  └────┘ └────┘ └────┘ └────┘ └────┘ └────┘               │
│                                                             │
│         [취소]                    [완료]                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 4.2.2 조립 메커니즘

| 액션 | 인터랙션 | 결과 |
|------|----------|------|
| **재료 추가** | 재료 버튼 클릭 또는 드래그 | 조립 영역에 추가 |
| **순서 맞추기** | 레시피 순서대로 추가 | 순서 보너스 +10% |
| **순서 틀림** | 순서 다르게 추가 | 순서 패널티 -10% |
| **잘못된 재료** | 레시피에 없는 재료 | 추가 불가 or 감점 |
| **취소** | 마지막 재료 제거 | 재료 반환 |

#### 4.2.3 양 조절 (선택적 확장)

```
간단한 버전: 클릭 = 1개 추가
확장 버전: 길게 누르기 = 양 조절 게이지

[양 조절 게이지]
┌────────────────────────────────┐
│░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░│ 0%
│████████░░░░░░░░░░░░░░░░░░░░░░│ 25% (적음)
│████████████████░░░░░░░░░░░░░░│ 50% (적당) ← 목표
│████████████████████████░░░░░░│ 75% (많음)
│██████████████████████████████│ 100% (넘침)
└────────────────────────────────┘
```

### 4.3 점수 계산

```lua
function calculateBuildScore(recipe, playerBuild)
    local score = 100

    -- 1. 재료 정확도
    for i, required in ipairs(recipe.ingredients) do
        local placed = playerBuild[i]
        if placed == nil then
            score = score - 20  -- 재료 누락
        elseif placed ~= required then
            score = score - 15  -- 잘못된 재료
        end
    end

    -- 2. 순서 보너스
    local correctOrder = true
    for i, required in ipairs(recipe.ingredients) do
        if playerBuild[i] ~= required then
            correctOrder = false
            break
        end
    end
    if correctOrder then
        score = score + 10  -- 순서 보너스
    end

    -- 3. 양 정확도 (확장 시)
    -- ...

    return math.max(0, math.min(100, score))
end
```

### 4.4 Exit Criteria

| ID | 체크 항목 | PASS 기준 |
|----|----------|----------|
| PG3-01 | 재료 선택 UI | 클릭/드래그로 재료 추가 |
| PG3-02 | 조립 시각화 | 추가된 재료 표시 |
| PG3-03 | 순서 판정 | 순서 맞으면 보너스 |
| PG3-04 | 취소 기능 | 마지막 재료 제거 |
| PG3-05 | 점수 연동 | buildScore 계산 |

---

## 5. Phase PG-4: Top/마무리 미니게임

### 5.1 개요

| 항목 | 내용 |
|------|------|
| **목표** | 토핑/소스 정밀 배치로 마무리 |
| **핵심 메커니즘** | 드래그로 소스 뿌리기, 토핑 위치 맞추기 |
| **영향 범위** | 신규 TopStation UI |
| **우선순위** | ⭐⭐ 높음 |

### 5.2 게임플레이 상세

#### 5.2.1 소스 뿌리기

```
┌─────────────────────────────────────────────────────────────┐
│                      Top Station                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│         [목표 패턴]           [내 패턴]                      │
│        ┌─────────┐          ┌─────────┐                    │
│        │ ~~~~~~~ │          │ ~~~     │                    │
│        │ ~~~~~~~ │          │  ~~~~   │                    │
│        │ ~~~~~~~ │          │         │                    │
│        └─────────┘          └─────────┘                    │
│                                                             │
│        [케첩]을 지그재그로 뿌리세요!                         │
│                                                             │
│        정확도: 72%                                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 5.2.2 토핑 배치

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│    [목표 위치]                [드래그해서 배치]              │
│    ┌─────────┐               ┌─────────┐                   │
│    │    🍅   │               │         │                   │
│    │  🧀  🧀 │               │  🧀     │ ← 드래그 중       │
│    │    🥬   │               │    🥬   │                   │
│    └─────────┘               └─────────┘                   │
│                                                             │
│    [토핑 목록]                                              │
│    🍅 x2    🧀 x2    🥬 x1                                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 5.3 정확도 계산

```lua
function calculateTopScore(targetPattern, playerPattern)
    -- 소스: 패턴 유사도 (간단한 영역 비교)
    -- 토핑: 목표 위치와의 거리

    local score = 100

    for i, target in ipairs(targetPositions) do
        local placed = playerPositions[i]
        if placed then
            local distance = (target - placed).Magnitude
            if distance < 10 then
                -- Perfect
            elseif distance < 20 then
                score = score - 5
            elseif distance < 30 then
                score = score - 10
            else
                score = score - 20
            end
        else
            score = score - 25  -- 토핑 누락
        end
    end

    return math.max(0, score)
end
```

### 5.4 Exit Criteria

| ID | 체크 항목 | PASS 기준 |
|----|----------|----------|
| PG4-01 | 소스 드래그 | 드래그로 패턴 그리기 |
| PG4-02 | 토핑 배치 | 드래그로 위치 지정 |
| PG4-03 | 정확도 표시 | 실시간 정확도 % |
| PG4-04 | 점수 연동 | topScore 계산 |

---

## 6. Phase PG-5: 시각적 피드백

### 6.1 개요

| 항목 | 내용 |
|------|------|
| **목표** | 숫자만 바뀜 → 실제로 만들어지는 시각적 만족감 |
| **핵심 메커니즘** | 애니메이션, 이펙트, 전환 |
| **영향 범위** | 모든 Station UI |
| **우선순위** | ⭐ 중간 |

### 6.2 애니메이션 목록

| 상황 | 애니메이션 | 설명 |
|------|-----------|------|
| **재료 추가** | 위에서 떨어짐 | 재료가 버거에 쌓임 |
| **요리 중** | 지글지글 + 김 | 패티 굽는 효과 |
| **Perfect 판정** | 별 터짐 + 흔들림 | 성공 강조 |
| **요리 완성** | 빛남 + 확대 | 완성 강조 |
| **서빙** | 슬라이드 | 손님에게 이동 |
| **팁 획득** | 동전 튀어오름 | 보상 강조 |
| **레벨업** | 광채 + 팡파레 | 해금 강조 |

### 6.3 이펙트 목록

| 이펙트 | 적용 상황 | 에셋 타입 |
|--------|----------|----------|
| 별/스파클 | Perfect 판정 | ParticleEmitter |
| 김/연기 | 요리 중 | ParticleEmitter |
| 기름 튀김 | 패티 굽기 | ParticleEmitter |
| 동전 | 팁 획득 | GUI 애니메이션 |
| 하트 | 손님 만족 | BillboardGui |

### 6.4 Exit Criteria

| ID | 체크 항목 | PASS 기준 |
|----|----------|----------|
| PG5-01 | 재료 애니메이션 | 쌓이는 시각 효과 |
| PG5-02 | 요리 이펙트 | 김/연기 파티클 |
| PG5-03 | 판정 피드백 | Perfect 시 강조 |
| PG5-04 | 완성 피드백 | 완성 시 효과 |
| PG5-05 | 보상 피드백 | 팁 획득 애니메이션 |

---

## 7. Phase PG-6: 멀티태스킹

### 7.1 개요

| 항목 | 내용 |
|------|------|
| **목표** | 한 번에 하나 → 여러 주문 동시 처리 |
| **핵심 메커니즘** | 스테이션 전환, 요리 보관, 우선순위 판단 |
| **영향 범위** | 전체 게임 루프 |
| **우선순위** | ⭐⭐ 높음 |

### 7.2 멀티태스킹 흐름

```
┌─────────────────────────────────────────────────────────────┐
│                    멀티태스킹 시나리오                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  손님1: [Build 완료] ──► [Cook 진행 중: 3초 남음]            │
│                              │                              │
│  손님2: [Order 대기] ──► [인내심: 60%] ← 먼저 Build?        │
│                              │                              │
│  손님3: [Order 대기] ──► [인내심: 30%] ← 급함!              │
│                                                             │
│  ──────────────────────────────────────────────────────     │
│                                                             │
│  플레이어 판단:                                              │
│  1. 손님3 먼저 Build 시작 (급하니까)                         │
│  2. 손님1 Cook 완료되면 바로 처리                            │
│  3. 손님2는 느긋하니까 나중에                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 7.3 스테이션 전환 UI

```
┌─────────────────────────────────────────────────────────────┐
│  [Order]    [Build]    [Cook]    [Top]    [Serve]          │
│     ●          ○         ◐        ○         ○              │
│   3명 대기   비어있음   1개 진행  비어있음  비어있음          │
└─────────────────────────────────────────────────────────────┘
│                                                             │
│            현재 스테이션: [Build]                            │
│                                                             │
│   ┌─────────────────────────────────────────┐              │
│   │                                         │              │
│   │         (Build UI 내용)                 │              │
│   │                                         │              │
│   └─────────────────────────────────────────┘              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 7.4 진행 중 요리 관리

```lua
-- 진행 중인 요리 슬롯
local cookingSlots = {
    [1] = {
        customerId = 1,
        recipeId = 5,
        stage = "COOKING",      -- BUILD / COOKING / TOPPING / READY
        buildScore = 85,
        cookProgress = 0.7,     -- Cook 미니게임 대기 중
        topScore = nil,
    },
    [2] = {
        customerId = 3,
        recipeId = 2,
        stage = "BUILD",
        -- ...
    },
}
```

### 7.5 Exit Criteria

| ID | 체크 항목 | PASS 기준 |
|----|----------|----------|
| PG6-01 | 스테이션 전환 | 탭으로 스테이션 변경 |
| PG6-02 | 동시 진행 | 2개 이상 요리 동시 |
| PG6-03 | 진행 상태 표시 | 각 요리 현재 단계 |
| PG6-04 | Cook 알림 | Cook 완료 시 알림 |
| PG6-05 | 우선순위 UI | 인내심 기반 정렬 |

---

## 8. Phase PG-7: Day 시스템 강화

### 8.1 개요

| 항목 | 내용 |
|------|------|
| **목표** | 무한 반복 → 하루 단위 진행감 |
| **핵심 메커니즘** | Day 목표, 러시아워, 해금 |
| **영향 범위** | DayService, DayEnd UI |
| **우선순위** | ⭐ 중간 |

### 8.2 Day 구조

```
┌─────────────────────────────────────────────────────────────┐
│                        DAY 5                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  [오프닝]                                                   │
│  "Day 5 시작! 오늘 목표: 팁 $500"                           │
│  "새 손님 '바쁜 밥'이 등장합니다!"                           │
│                                                             │
│  [게임플레이]                                               │
│  ├─ Phase 1: 한산 (손님 1~2명씩)                            │
│  ├─ Phase 2: 보통 (손님 2~3명씩)                            │
│  ├─ Phase 3: 러시! (손님 3명 연속)                          │
│  └─ Phase 4: 마무리 (잔여 손님)                             │
│                                                             │
│  [Day End]                                                  │
│  ├─ 통계: 손님 12명, 평균 85점                              │
│  ├─ 수입: $342 (목표 $500 실패)                             │
│  ├─ 해금: 없음                                              │
│  └─ "내일 다시 도전!"                                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 8.3 Day 설정 테이블

```lua
DayConfig = {
    [1] = {
        day = 1,
        customerCount = 5,
        rushStart = 3,       -- 3번째 손님부터 러시
        rushIntensity = 1,   -- 동시 손님 수
        tipGoal = 100,
        unlocks = {},
    },
    [5] = {
        day = 5,
        customerCount = 12,
        rushStart = 6,
        rushIntensity = 2,
        tipGoal = 500,
        unlocks = {
            customers = {2},  -- "바쁜 밥" 해금
            recipes = {},
            ingredients = {},
        },
    },
    [10] = {
        day = 10,
        customerCount = 20,
        rushStart = 10,
        rushIntensity = 3,
        tipGoal = 1000,
        unlocks = {
            customers = {5},
            recipes = {15},
            ingredients = {6},  -- Bacon 해금
        },
    },
}
```

### 8.4 Exit Criteria

| ID | 체크 항목 | PASS 기준 |
|----|----------|----------|
| PG7-01 | Day 목표 | 시작 시 목표 표시 |
| PG7-02 | 러시아워 | 손님 몰림 구현 |
| PG7-03 | 해금 알림 | Day End 시 해금 표시 |
| PG7-04 | 난이도 증가 | Day↑ → 손님↑, 러시↑ |

---

## 9. Phase PG-8: 폴리시 (Polish)

### 9.1 개요

| 항목 | 내용 |
|------|------|
| **목표** | 기능 완성 → 느낌 완성 (Game Juice) |
| **핵심 메커니즘** | 사운드, 파티클, 피드백 |
| **영향 범위** | 전체 |
| **우선순위** | ⭐ 마무리 |

### 9.2 사운드 목록

| 카테고리 | 사운드 | 재생 시점 |
|----------|--------|----------|
| **UI** | 클릭음 | 버튼 클릭 |
| **UI** | 탭 전환음 | 스테이션 변경 |
| **요리** | 지글지글 | Cook 진행 중 |
| **요리** | 완성음 | 요리 완성 |
| **판정** | Perfect 효과음 | Perfect 판정 |
| **판정** | Bad 효과음 | Bad 판정 |
| **보상** | 코인 소리 | 팁 획득 |
| **해금** | 팡파레 | 새 콘텐츠 해금 |
| **손님** | 만족 소리 | 높은 점수 |
| **손님** | 불만 소리 | 낮은 점수 |
| **BGM** | 게임 BGM | 게임 중 루프 |
| **BGM** | Day End BGM | Day End 화면 |

### 9.3 Game Juice 요소

| 요소 | 적용 | 효과 |
|------|------|------|
| **화면 흔들림** | Perfect 판정 | 강조 |
| **슬로우모션** | 타이밍 성공 직후 0.2초 | 쾌감 |
| **확대** | 요리 완성 | 만족감 |
| **컬러 플래시** | 판정 순간 | 피드백 |
| **숫자 팝업** | 점수 획득 | 즉각 피드백 |
| **트레일** | 드래그 | 조작감 |

### 9.4 Exit Criteria

| ID | 체크 항목 | PASS 기준 |
|----|----------|----------|
| PG8-01 | UI 사운드 | 클릭/전환음 |
| PG8-02 | 요리 사운드 | 지글/완성음 |
| PG8-03 | 판정 피드백 | 시청각 피드백 |
| PG8-04 | BGM | 루프 재생 |
| PG8-05 | Game Juice | 흔들림/확대 등 |

---

## 10. 구현 로드맵 요약

### 10.1 권장 순서

```
Phase 1: 최소 재미 (MVP)
├─ PG-1: Cook 미니게임 ──────────┐
└─ PG-2: 손님 시스템 ────────────┴─► "게임 같은 느낌" 시작

Phase 2: 깊이 추가
├─ PG-3: Build 미니게임 ─────────┐
└─ PG-6: 멀티태스킹 ─────────────┴─► 전략적 요소

Phase 3: 만족감
├─ PG-4: Top 미니게임 ───────────┐
└─ PG-5: 시각적 피드백 ──────────┴─► 시각적 만족

Phase 4: 완성도
├─ PG-7: Day 시스템 ─────────────┐
└─ PG-8: 폴리시 ─────────────────┴─► 출시 품질
```

### 10.2 예상 일정 (참고용)

| Phase | 예상 기간 | 누적 |
|-------|----------|------|
| PG-1 | 3~5일 | 1주 |
| PG-2 | 5~7일 | 2주 |
| PG-3 | 3~5일 | 3주 |
| PG-4 | 3~5일 | 3.5주 |
| PG-5 | 3~5일 | 4주 |
| PG-6 | 5~7일 | 5주 |
| PG-7 | 3~5일 | 6주 |
| PG-8 | 3~5일 | 6.5주 |

> **Note**: 일정은 참고용이며, 실제는 상황에 따라 다름

### 10.3 최소 출시 가능 버전

**PG-1 + PG-2 완료 시:**
- 타이밍 미니게임 있음
- 손님 캐릭터 있음
- 인내심 시스템 있음
- → "게임"이라 부를 수 있음

---

## 11. 기존 시스템과의 관계

### 11.1 기존 Phase와의 관계

| 기존 Phase | 재미 복제 관계 |
|------------|---------------|
| P0~P4 | 시스템 기반 (유지) |
| P5/W2/W3 | 콘텐츠 (유지) |
| P6 | 운영 품질 (PG 이후 진행) |
| P7 | TypeTag (PG 이후 선택) |

### 11.2 병합 가능성

- PG는 기존 P0~P5의 **확장**
- 기존 코드 대부분 **유지**, UI/인터랙션 **추가**
- Contract 변경 최소화

### 11.3 회귀 방지

- 기존 저장/로드 로직 유지
- 기존 레시피 데이터 유지
- 기존 업그레이드 로직 유지

### 11.4 기존 업그레이드 연동 (Q1 결정)

> **감리자 결정 (2026-01-20)**: CraftSpeed 업그레이드 = 게이지 속도 감소

**배경:**
- 기존 CraftSpeed: `BaseTime * 0.9^(level-1)` (단순 시간 감소)
- PG 미니게임 도입 시 업그레이드 역할 재정의 필요

**결정:**
| 업그레이드 | 기존 효과 | PG 효과 |
|------------|----------|---------|
| CraftSpeed | craftTime 감소 | **게이지 속도 감소** (더 쉬움) |

**수식:**
```lua
-- CraftSpeed 레벨에 따른 게이지 속도 배율
local speedMultiplier = math.max(0.6, 1.0 * 0.9^(craftSpeedLevel - 1))
-- Level 1: 1.0 (기본)
-- Level 2: 0.9
-- Level 3: 0.81
-- Level 4: 0.729
-- Level 5: 0.656
-- Level 6+: 0.6 (하한선)

local actualGaugeSpeed = baseGaugeSpeed * speedMultiplier
```

**가드레일:**
- `speedMultiplier ≥ 0.6` (하한선)
- **Max Level = 5** (Owner 결정 2026-01-20)
- 과도한 업그레이드로 Perfect 윈도우 체감 과잉 방지

**하한선 0.6 근거 (Owner 결정 2026-01-20):**
- **튜토리얼 Perfect 80% 달성 실험값** 기준
- PG1-07 Exit Criteria(튜토리얼 Perfect 80% 달성)와 직접 연결
- 향후 밸런스 조정 시 기준점으로 활용

**Max Level = 5 근거:**
- Level 5에서 speedMultiplier = 0.656 → 하한선(0.6) 직전
- 숙련이 여전히 의미 있음 (Perfect 80% 가능, 100%는 아님)
- 업그레이드 동기 유지, "완전 자동화" 방지

> **Note (감리자 권고)**: Max Level = 5 기준으로, 0.6 하한선은 **안전장치**로 존재하며
> 정상 진행에서는 도달하지 않습니다. 향후 Max Level 변경 시에만 의미를 가집니다.

**CraftSpeed 시각 피드백 분리 (Owner 채택 2026-01-20):**

> 숫자 변화 → 감각 변화로 업그레이드 만족도 상승

| Level | speedMultiplier | 시각 피드백 |
|-------|-----------------|------------|
| 1 | 1.0 | 기본 색상, 기본 틱 속도 |
| 2 | 0.9 | 게이지 색 약간 밝음 |
| 3 | 0.81 | 게이지 색 더 밝음, 틱 느려짐 |
| 4 | 0.729 | 황금색 틴트 |
| 5 | 0.656 | 황금색 + 부드러운 글로우 |

**구현 시점:** PG-5 (시각적 피드백) 또는 PG-8 (폴리시)

### 11.5 Anti-Pattern 예외 근거 (Q2 결정)

> **감리자 결정 (2026-01-20)**: AND 해석 채택

**CLAUDE.md §16.3 원문:**
> "3D 물리/이동/충돌 기반 실패·숙련 요구"

**해석 고정:**
```
금지 대상 = "3D 물리/이동/충돌 기반" AND "실패·숙련 요구"

∴ 2D UI 기반 타이밍/정밀도 미니게임은 Anti-Pattern에 해당하지 않는다.
```

**PG 미니게임 판정:**
| 항목 | PG Cook 미니게임 | 판정 |
|------|-----------------|------|
| 3D 물리 | ❌ 2D UI 게이지 | 해당 없음 |
| 이동/충돌 | ❌ 타이밍만 | 해당 없음 |
| 숙련 요구 | ⚠️ 타이밍 스킬 | **예외 허용** |

**예외 근거:**
1. Papa's 복제 목표 자체가 타이밍/정밀도 기반
2. 튜토리얼 Perfect ±8 확대로 진입장벽 완화
3. 업그레이드로 난이도 조절 가능

### 11.6 기존 인프라 활용 (Q3 결정)

> **감리자 결정 (2026-01-20)**: 기존 `foodtruck-log-proxy` Workers 확장

**P6-P2 SEALED 인프라:**
```
Roblox ProductionLogger
    ↓ HTTP POST
Cloudflare Workers (foodtruck-log-proxy)  ← 여기 확장
    ↓ POST + redirect follow
Google Apps Script
    ↓
Google Sheets (Evidence)
```

**PG 확장 내용:**
| 기존 (P6-P2) | PG 추가 |
|-------------|---------|
| `GETASYNC_RESULT` | `COOK_JUDGMENT` |
| `SAVE_START` | `CUSTOMER_SERVE` |
| `SAVE_OK` | `CUSTOMER_LEAVE`, `RUSH_*`, `DAY_END` |
| uid 직접 저장 | uid → **uid_hash 변환** (Workers에서) |

**Workers 확장 코드 (§13.2.0 참조):**
```javascript
// 기존 /log 핸들러에 추가
if (body.uid) {
    body.uid_hash = await hashUserId(body.uid);
    delete body.uid;
}
```

**장점:**
- Evidence 파이프라인 단일화
- 관리 포인트 최소화
- P6-P2에서 검증된 안정성 유지

---

## 13. 텔레메트리 이벤트 스키마 (M4)

> ⚠️ **필수 조건**: 밸런싱/분석용 데이터 수집

### 13.1 이벤트 목록

| 이벤트 | 발생 시점 | 용도 |
|--------|----------|------|
| `COOK_JUDGMENT` | Cook 미니게임 판정 완료 | 판정 분포 분석 |
| `CUSTOMER_SERVE` | 손님에게 서빙 완료 | 점수/팁 분석 |
| `CUSTOMER_LEAVE` | 손님 이탈 (인내심 0) | 이탈율 분석 |
| `RUSH_START` | Rush 시작 | Rush 빈도 분석 |
| `RUSH_END` | Rush 종료 | Rush 성과 분석 |
| `DAY_END` | Day 종료 | Day별 통계 |

### 13.2 이벤트 스키마

> ⚠️ **v0.3 변경**: `uid` 원문 저장 금지 → `uid_hash` 사용 (개인정보 보호)

#### 13.2.0 uid_hash 생성 (v0.3.1 개정)

> ⚠️ **현실성 보완**: Roblox에 SHA256 내장 없음 → Workers 처리 방식 채택

**아키텍처:**
```
Roblox (UserId 전송) → Workers (SHA256 계산) → Apps Script → Sheets
                              ↑
                    crypto.subtle.digest() 사용
```

**Roblox 측 (uid만 전송):**
```lua
-- ProductionLogger.lua: 원본 UserId 전송 (Workers에서 해시 처리)
local function SendLogToWorkers(logData, player)
    logData.uid = player.UserId  -- 내부 통신용 (Workers까지만)
    -- Workers가 uid → uid_hash 변환 후 Apps Script로 전달
    HttpService:PostAsync(WORKERS_URL, HttpService:JSONEncode(logData))
end
```

**Workers 측 (해시 처리):**
```javascript
// Cloudflare Workers: SHA256 해시 후 전달
async function hashUserId(uid) {
    const secret = "FoodTruck_Evidence_Salt_v1";
    const payload = secret + ":" + uid;
    const encoder = new TextEncoder();
    const data = encoder.encode(payload);
    const hashBuffer = await crypto.subtle.digest('SHA-256', data);
    const hashArray = Array.from(new Uint8Array(hashBuffer));
    const hashHex = hashArray.map(b => b.toString(16).padStart(2, '0')).join('');
    return hashHex.substring(0, 16);  // 앞 16자
}

// /log 핸들러에서:
body.uid_hash = await hashUserId(body.uid);
delete body.uid;  // 원본 삭제
// → Apps Script로 uid_hash만 전달
```

**결과:**
- Roblox ↔ Workers: 내부 통신 (uid 노출 OK)
- Workers → 외부: **uid_hash만** (원본 UserId 없음)
- External Evidence: 개인정보 보호 충족

#### 13.2.1 COOK_JUDGMENT (v0.3.1 확장)

> ⚠️ **감리자 필수 조건**: 불일치 발생률 추적용 필드 추가

```lua
{
    event = "COOK_JUDGMENT",
    timestamp = os.clock(),
    uid_hash = GetUidHash(player),   -- v0.3: 원문 UserId 저장 금지
    day = currentDay,
    recipeId = recipe.id,
    difficulty = recipe.difficulty,  -- Easy/Medium/Hard
    isTutorial = false,              -- v0.3.1: 튜토리얼 여부

    -- 판정 (v0.3.1: 클라/서버 분리)
    clientJudgment = "Perfect",      -- 클라 임시 판정 (optional, UI 표시용)
    serverJudgment = "Great",        -- 서버 확정 판정 (진실)
    corrected = true,                -- 불일치 여부 (clientJudgment ~= serverJudgment)

    -- 측정값
    serverGaugePos = 52.3,           -- 서버 계산 게이지 위치 (0~100)
    cookTime = 3.2,                  -- 조리 시간 (초)
    rtt_ms = 85,                     -- 클라 요청~서버 응답 왕복 시간 (ms)
}
```

**보정 방향 분류:**
| corrected | 방향 | 설명 | UX 처리 |
|-----------|------|------|---------|
| false | - | 일치 | 그대로 표시 |
| true | Down | 클라 Perfect → 서버 Great | 묵시적 보정 (§2.8.4) |
| true | **Up** | 클라 Good → 서버 Great | **좋아지는 보정** (자연스럽게 업) |

> **지적 반영**: Up-correct는 불만이 적으므로 시각적으로 자연스럽게 올려주면 됨

#### 13.2.2 CUSTOMER_SERVE

```lua
{
    event = "CUSTOMER_SERVE",
    timestamp = os.clock(),
    uid_hash = GetUidHash(player),   -- v0.3: 원문 UserId 저장 금지
    day = currentDay,
    customerId = customer.id,
    customerType = "Busy",           -- Regular/Busy/Patient/VIP
    recipeId = recipe.id,
    buildScore = 85,                 -- 0~100
    cookScore = 95,                  -- 0~100 (또는 nil if PG-1 미적용)
    topScore = 70,                   -- 0~100 (또는 nil if PG-4 미적용)
    patienceRemaining = 45,          -- 0~100 (% 표시)
    totalScore = 82,                 -- 가중 평균
    tipEarned = 15,                  -- $
    waitTime = 25.3,                 -- 초
}
```

#### 13.2.3 RUSH_START / RUSH_END

```lua
{
    event = "RUSH_START",  -- 또는 "RUSH_END"
    timestamp = os.clock(),
    uid_hash = GetUidHash(player),   -- v0.3: 원문 UserId 저장 금지
    day = currentDay,
    rushNumber = 2,          -- Day 내 몇 번째 Rush
    queueSize = 4,           -- Rush 시작 시 대기 손님 수
    -- RUSH_END 추가 필드
    servedDuringRush = 5,    -- Rush 중 서빙한 손님 수
    avgScoreDuringRush = 78, -- Rush 중 평균 점수
}
```

### 13.3 수집 방식

```lua
-- ProductionLogger 활용 (P6 파이프라인 재사용)
local ProductionLogger = require(ServerScriptService.Server.Shared.ProductionLogger)

function LogGameEvent(event)
    if not CONFIG.IS_STUDIO then
        ProductionLogger:Log(event)
    else
        -- Studio에서는 로컬 로그만
        print("[TELEMETRY]", HttpService:JSONEncode(event))
    end
end
```

### 13.4 분석 지표

| 지표 | 계산식 | 목표 |
|------|--------|------|
| Perfect 비율 | COOK_JUDGMENT 중 Perfect / 전체 | 30~50% |
| 평균 점수 | CUSTOMER_SERVE의 totalScore 평균 | 70~85 |
| 이탈율 | CUSTOMER_LEAVE / 전체 손님 | < 10% |
| Rush 생존율 | Rush 중 서빙 / Rush 중 도착 | > 80% |

---

## 14. UI/UX 가이드라인 (M5)

> ⚠️ **필수 조건**: 모바일 우선 설계

### 14.1 터치 영역

| 요소 | 최소 크기 | 권장 크기 |
|------|----------|----------|
| 버튼 | 44x44 px | 48x48 px |
| 탭 영역 | 44x44 px | 56x56 px |
| 게이지 COOK 버튼 | 60x60 px | 80x80 px |
| 재료 아이콘 | 48x48 px | 56x56 px |

### 14.2 레이아웃 원칙

```
┌─────────────────────────────────────────────────────────────┐
│  [상단 HUD]                                                 │
│  Day, 잔액, 점수                                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                    [메인 게임 영역]                          │
│                    높이: 60% 이상                           │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  [하단 액션 영역]                                           │
│  터치 버튼, 재료 선택                                       │
│  높이: 25% (엄지 접근성)                                    │
└─────────────────────────────────────────────────────────────┘
```

### 14.3 피드백 원칙

| 피드백 유형 | 시간 | 예시 |
|-------------|------|------|
| 터치 인식 | 즉시 (<16ms) | 버튼 하이라이트 |
| 판정 결과 | 즉시 (<50ms) | Perfect 텍스트 팝업 |
| 애니메이션 | 200~500ms | 별 터짐, 점수 상승 |
| 전환 효과 | 300ms | 스테이션 전환 |

### 14.4 표현 규칙 (v0.3 필수)

> ⚠️ **필수 조건**: 모든 판정/상태는 **색상 + 텍스트 + 아이콘** 3중 표현

#### 14.4.1 판정 표현 (색상만 금지)

| 판정 | 색상 | 텍스트 | 아이콘 |
|------|------|--------|--------|
| **Perfect** | 금색 `#FFD700` | "Perfect!" | ⭐ (별) |
| **Great** | 초록 `#00FF00` | "Great!" | ★ (채운별) |
| **Good** | 연두 `#7FFF00` | "Good" | ✓ (체크) |
| **OK** | 노랑 `#FFFF00` | "OK" | ○ (원) |
| **Bad** | 빨강 `#FF4444` | "Bad" | ✗ (엑스) |

> **색상만으로 판정을 전달하면 안 됩니다.** 반드시 텍스트 + 아이콘 병행.

#### 14.4.2 인내심 표현

| 인내심 | 색상 | 표정 | 아이콘 |
|--------|------|------|--------|
| 80~100% | 초록 `#00FF00` | 😊 | ♥♥♥ |
| 60~79% | 연초록 `#7FFF00` | 🙂 | ♥♥ |
| 40~59% | 노랑 `#FFFF00` | 😐 | ♥ |
| 20~39% | 주황 `#FFA500` | 😕 | ◇ |
| 0~19% | 빨강 `#FF4444` | 😠 | ✗ |

> 인내심 게이지는 **색상 + 표정 이모지 + 하트 아이콘** 3중 표시.

### 14.5 접근성

| 항목 | 정책 |
|------|------|
| **색맹 대응** | **색상 + 텍스트 + 아이콘 3중 표현 (필수)** |
| 글자 크기 | 최소 14pt (모바일) |
| 대비 | WCAG AA 준수 (4.5:1) |
| 애니메이션 | 과도한 플래시 지양 |

> **주의**: 빨강-초록 색맹(적록색맹)은 인구의 8%입니다. 색상만으로 구분하면 안 됩니다.

### 14.6 PC 호환

| 입력 | 모바일 | PC |
|------|--------|-----|
| COOK 버튼 | 탭 | 클릭 또는 Space |
| 재료 선택 | 탭 | 클릭 또는 1~6 키 |
| 스테이션 전환 | 탭 | 클릭 또는 Q/E 키 |
| 취소 | 탭 | 클릭 또는 Backspace |

---

## 15. 부록

### A. Papa's 시리즈 참고 자료

| 게임 | 특징 | 참고 요소 |
|------|------|----------|
| Papa's Freezeria | 아이스크림 | Mix 게이지, 토핑 |
| Papa's Pizzeria | 피자 | Build 순서, 오븐 타이밍 |
| Papa's Burgeria | 버거 | Build 쌓기, 그릴 타이밍 |
| Papa's Taco Mia | 타코 | 재료 양 조절 |

### B. 용어 정의

| 용어 | 정의 |
|------|------|
| **Cook 미니게임** | craftTime 동안의 타이밍 게이지 |
| **Build 미니게임** | 재료 조립 인터랙션 |
| **Top 미니게임** | 토핑/소스 배치 인터랙션 |
| **인내심** | 손님 대기 시간에 따른 만족도 |
| **멀티태스킹** | 여러 주문 동시 처리 |
| **Game Juice** | 화면 효과, 사운드 등 피드백 요소 |

### C. 변경 이력

| Version | Date | Changes |
|---------|------|---------|
| v0.1 | 2026-01-20 | 초안 작성 (Claude) |
| v0.2 | 2026-01-20 | 감리자 조건부 승인 반영 |
| v0.3 | 2026-01-20 | 감리자 피드백 6건 수정 완료 (조건 충족) |
| v0.3.1 | 2026-01-20 | 감리자 최종 검토 4건 수정 (PG-1 착수 준비 완료) |
| **v0.3.2** | **2026-01-20** | **Owner 최종 결정 5건 반영 (실패 정의, Max Level, 보호 구간)** |

#### v0.3.1 변경사항 상세 (감리자 최종 검토 반영)

**필수 수정 1 - PG2-09 Patience 범위:**
- "0~100 Clamp" → **`currentPatience ∈ [0, patienceMax], UI는 % 표시`**
- §3.8 patienceMax 모델과 정합성 확보

**필수 수정 2 - §13.2.0 uid_hash:**
- 예시 코드의 가짜 해시 → **SHA256(secret + ":" + UserId) 정식 구현**
- 앞 16자 사용, SHA256 모듈 require 명시

**중요 수정 3 - §2.7.2 ServerStartTime:**
- 클라이언트에 전송 → **제거**
- 서버 내부 시간 노출 금지 (보안)

**중요 수정 4 - §13.2.1 gaugePosition:**
- `gaugePosition` → **`serverGaugePos`**
- 텔레메트리에서 서버 계산 값임을 명시

**추가 - §2.8.4 UX 보정 정책:**
- 절충안(Compromise) 채택
- 튜토리얼 1회 안내 + 이후 묵시적 처리

**추가 (감리자 2차 검토 반영):**

**(A) §13.2.0 uid_hash 현실성 보완:**
- Roblox에 SHA256 모듈 없음 확인
- Workers에서 `crypto.subtle.digest('SHA-256')` 처리로 변경
- Roblox → Workers: uid 전송, Workers → External: uid_hash만

**(B) §2.7.4 gaugeSpeed 참조 수정:**
- `self.gaugeSpeed` → **인자로 전달** (`gaugeSpeed`)
- 함수 시그니처 변경: `(gaugePos, targetZone, gaugeSpeed, latencyMs)`
- 호출부: `session.gaugeSpeed` 전달

**추가 (감리자 옵션 1 채택 + 필수 조건 4개):**

**(C) 클라 임시 판정 방식 결정:**
- **옵션 1 채택**: 클라 로컬 게이지로 임시 판정 (즉각 피드백)
- Papa's 복제 목표와 정합, 서버 200~300ms 대기 체감 품질 저하 방지

**(D) 임시 판정 UI 전용 명시 (§2.7.7):**
- 클라 임시 판정 = 애니메이션/텍스트만
- 점수/팁/진행 = **서버 확정만 진실**
- Up-correct 처리 추가 (클라 Good → 서버 Great: 자연스럽게 업)

**(E) COOK_JUDGMENT 텔레메트리 확장 (§13.2.1):**
- `clientJudgment` (optional): 클라 임시 판정
- `serverJudgment`: 서버 확정 판정
- `corrected`: 불일치 여부
- `rtt_ms`: 왕복 시간 (LATENCY_WINDOW 조정 근거)
- `isTutorial`: 튜토리얼 여부

**(F) 튜토리얼 Perfect 윈도우 확대 (§2.7.5):**
- 튜토리얼: ±8 / 일반: ±5
- `JUDGMENT_ZONES` 테이블 도입
- 함수 시그니처에 `isTutorial` 인자 추가
- 세션에 `isTutorial = not playerData.tutorialComplete` 저장

**추가 (CLAUDE.md 정합성 검토 반영):**

**(G) §11.4 CraftSpeed 업그레이드 연동 (Q1):**
- CraftSpeed → 게이지 속도 감소 결정
- `speedMultiplier = max(0.6, 0.9^(level-1))` 수식
- 하한선 0.6 가드레일

**(H) §11.5 Anti-Pattern 예외 근거 (Q2):**
- AND 해석 고정: "3D 물리 기반" AND "숙련 요구"
- 2D UI 타이밍 미니게임은 예외
- CLAUDE.md §16.3에 해석 고정 문구 추가

**(I) §11.6 기존 인프라 활용 (Q3):**
- P6-P2 `foodtruck-log-proxy` Workers 확장
- uid_hash 변환 로직 추가
- Evidence 파이프라인 단일화

**(J) CLAUDE.md 업데이트:**
- §18 PG 로드맵 섹션 신규 추가
- 헤더 Current Phase: PG-1 착수 준비
- Next Step: PG-1 구현 착수

**추가 (Owner 최종 결정 2026-01-20):**

**(K) §2.2.2a 실패 정의:**
- **Bad = 점수/팁 감소 + 표정 악화, 플레이는 계속**
- Anti-Pattern "Game Over 금지" 준수
- cookScore × 0.25 (최저 배율)

**(L) §2.2.2b Perfect 보호 구간:**
- 서버 판정 Perfect→Great 다운 시 중심 ±1 보호
- 네트워크 불만 감소 UX
- `PROTECTION_EPSILON = 1` 도입

**(M) §11.4 CraftSpeed Max Level:**
- **Max Level = 5** 확정
- Level 5 = speedMultiplier 0.656 (하한선 직전)
- 숙련 의미 유지, 완전 자동화 방지

**(N) §11.4 CraftSpeed 시각 피드백 분리:**
- Level별 게이지 색상/글로우 차등
- 숫자 변화 → 감각 변화로 만족도 상승
- 구현 시점: PG-5 또는 PG-8

**(O) §11.4 하한선 0.6 근거 명시:**
- **튜토리얼 Perfect 80% 달성 실험값** 기준
- PG1-07 Exit Criteria와 직접 연결

---

#### v0.3 변경사항 상세 (감리자 피드백 반영)

**수정 (A) - §2.7 서버 검증 취약점:**
- 클라 `gaugePosition` 신뢰 → **서버 독립 재현 방식**으로 전면 재작성
- seed + serverStartTime 기반, 클라는 tapTime만 전송 (선택)
- 네트워크 지연 보정 윈도우 (±80ms) 추가

**수정 (B) - PG1-07 Perfect 목표 충돌:**
- "80% 달성 가능" → **난이도별 분리**
- 튜토리얼: 60~80% / 일반: 30~50% / Hard: 10~25%
- §13.4와 정합성 확보

**수정 (C) - PG1-08 100ms 측정 불명확:**
- **클라/서버 분리**: 클라 피드백 <50ms, 서버 확정 <300ms
- 임시 판정 → 서버 확정 → 보정 처리 흐름 명시

**수정 (D) - Patience 설계 충돌:**
- patience=150 vs Clamp 0~100 모순 → **patienceMax 모델**로 통일
- §3.2, §3.8 전체 수정
- UI는 `currentPatience / patienceMax * 100`으로 % 표시

**수정 (E) - §14 색상만 판정 전달:**
- 색상 + 텍스트 + 아이콘 **3중 표현 필수** 명시
- 적록색맹 대응 주의사항 추가

**수정 (F) - §13 uid 원문 저장:**
- `uid = player.UserId` → **`uid_hash = GetUidHash(player)`**
- 외부 Evidence에 원문 UserId 저장 금지

---

#### v0.2 변경사항 (참조)

- Status: DRAFT → APPROVED (조건부)
- §0.5 플랫폼 정책, §0.6 확정 결정사항, §0.7 필수 조건 추가
- §2.7 서버 검증, §2.8 입력 보정, §3.7 Rush, §3.8 Patience 추가
- §13 텔레메트리, §14 UI/UX 가이드라인 추가

---

**End of Document**
